{% extends '::base.html.twig' %}

{% block body %}
    
    <h1>Recette edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('recette') }}">
           Liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
